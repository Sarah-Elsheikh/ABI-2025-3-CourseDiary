# Notes about git

## Commands

- `git status` -- shows changes in 
- `git add` -- add changes
- `git commit` -- bundle staged changes
- `git push` --Sync changes [^changes] **to** remote
- `git pull` -- Sync changes **from** remote
- `git restore` 

## commit message 

- write a highlight on what changed
- write author
